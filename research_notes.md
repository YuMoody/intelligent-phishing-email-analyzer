Research Notes:
- Python's built-in `email` package can parse .eml files.
- `mail-parser` is another library that extracts headers, attachments, and URLs.
- Flask or FastAPI can use these libraries to process uploaded emails.
